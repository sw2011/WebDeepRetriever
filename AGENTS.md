# WebDeepRetriever Agent 开发原则

## 项目定位

WebDeepRetriever 是一个通用网页 Agent，而不是针对若干网站编写的自动化脚本集合。

面对任务时，应由 Agent 根据任务目标、当前页面语义、可交互元素、网络与文档证据，自主决定如何导航、操作、提取、验证并提交结果。项目代码的职责是为 Agent 提供充分、可靠、可组合的通用工具和清晰的状态反馈，而不是替 Agent 固化具体网站的操作流程。

## 核心原则

1. Agent 自主决策
   - 由 Agent 判断页面有什么内容、下一步执行什么操作、使用哪类证据以及何时完成任务。
   - 运行时不得通过预设网站流程、隐藏答案或任务编号映射替代 Agent 的判断。

2. 优先建设通用能力
   - 工具应围绕浏览器与网页的通用能力设计，例如观察、点击、填写、选择、滚动、标签页、下载、文档解析、结构化提取、视觉检查、网络证据和结果验证。
   - 页面状态、动作回执、错误分类和恢复信息必须足够明确，使 Agent 能根据反馈调整策略。
   - 新能力应可组合，并对多个无关网站和任务类型有价值。

3. 禁止网站特判
   - 默认禁止按域名、URL、网站名称、固定文案、固定 DOM 层级、固定选择器或固定接口编写专用分支。
   - 默认禁止为单个评测任务、`task_idx`、标准答案或已知页面流程增加规则。
   - 不得把网站专用知识隐式写入提示词、工具实现、验证器或错误恢复逻辑。

4. 从失败中提炼通用缺口
   - 真实网站失败首先用于判断 Agent 缺少哪种观察、操作、规划、证据或恢复能力。
   - 修复应落在最小的通用抽象层，而不是直接修补触发问题的网站。
   - 如果一个改动在目标网站更换域名、文案或 DOM 结构后就失去价值，它通常不应进入通用运行时。

5. 保持 Agent 的行动空间
   - 安全、熔断和验证机制应阻止确定的错误、伪完成和不可恢复状态，但不得把可恢复的工具失败过早升级为任务终止。
   - 工具失败应尽可能提供可操作的结构化反馈，让 Agent 能选择其他路径；只有真实终态不确定或浏览器不可用时才终止当前任务。

## 例外边界

只有以下情况可以引入兼容逻辑：

- 浏览器协议、HTML、ARIA、下载、文件格式或通用前端组件存在可复现的兼容差异；
- 方案基于能力检测或标准特征，而不是网站身份；
- 能通过多个无关网站或独立测试夹具证明其通用性；
- 无法通用化且确需网站专用支持时，必须先获得用户明确同意，不得自行加入。

## 迭代与调研顺序

后续迭代和调研按以下优先级进行：

1. Agent 是否获得了完成判断所需的页面语义和证据；
2. 通用工具是否缺失、表达不足或反馈不可操作；
3. 浏览器生命周期、动作终态、错误恢复和任务隔离是否正确；
4. 上下文组织、提示词和模型规划是否支持自主决策；
5. 网站差异只作为验证通用方案的样本，不作为直接添加特判的依据。

## 验收标准

- 功能测试不仅验证代码路径，还要验证 Agent 是否仍能自主选择不同操作方案。
- 通用能力修改应使用合成页面以及多个无关网站覆盖正常路径、边界条件和异常路径。
- 真实任务以有效完成率和答案正确率为核心指标；不得用更多步骤、更多工具调用或更严格的失败分类代替任务成功。
- 对任务成功率可能有影响的优化必须做改动前后对比；没有端到端证据时，不得宣称能力提升。
- 审查时必须检查是否引入网站身份判断、固定流程、隐藏任务知识或不必要地收窄 Agent 行动空间。
